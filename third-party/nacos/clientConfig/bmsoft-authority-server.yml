# 在这里配置 权限服务 所有环境都能使用的配置
bmsoft:
  swagger:
    enabled: true
    docket:
      auth:
        title: 权限模块
        base-package: com.bmsoft.cloud.authority.controller.auth
      common:
        title: 公共模块
        base-package: com.bmsoft.cloud.authority.controller.common
      core:
        title: 组织模块
        base-package: com.bmsoft.cloud.authority.controller.core
      tenant:
        title: 租户模块
        base-package: com.bmsoft.cloud.authority.controller.defaults

server:
  port: 8764
